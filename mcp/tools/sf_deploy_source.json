{
  "name": "sf_deploy_source",
  "description": "Deploy Salesforce metadata source to an org using SFDX",
  "input_schema": {
    "type": "object",
    "properties": {
      "sourceDir": { "type": "string", "description": "Path to metadata source directory (e.g., force-app)" },
      "orgAlias": { "type": "string", "description": "Target org alias" }
    },
    "required": ["sourceDir", "orgAlias"],
    "additionalProperties": false
  }
}



